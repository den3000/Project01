package ru.den.writes.code.agenticHub.features.fsm.task

class RetryReasonTest {

//    @Test
//    fun `when every reason is classified - then it maps to the level that spends it`() {
//        // given
//        val table = mapOf(
//            RetryReason.JUDGE_REWRITE to RetryLevel.TURN,
//            RetryReason.TRANSPORT_FAILED to RetryLevel.TRANSPORT,
//            RetryReason.NO_MARKER to RetryLevel.STAGE,
//            RetryReason.STAGE_REPEATED to RetryLevel.STAGE,
//            RetryReason.STAGE_REJECTED to RetryLevel.STAGE,
//            RetryReason.JUDGE_BLOCKED to RetryLevel.STAGE,
//            RetryReason.TASK_STALLED to RetryLevel.TASK,
//            RetryReason.USER_RESTART to RetryLevel.TASK,
//        )
//
//        // when - then
//        // forEach justified (rule §11.E): one invariant — "the reason spends the
//        // budget its level names" — over every enum value; the table is the taxonomy.
//        RetryReason.entries.forEach { reason ->
//            assertEquals(table[reason], reason.level, "level($reason)")
//        }
//    }
}
