#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M

#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true

#Android
android.nonTransitiveRClass=true
android.useAndroidX=true

# liveTests здесь НЕ включать: флаг снимает гейт live-тестов сразу у всех и на CI, и обычный
# прогон начинает бить по реальным сервисам и жечь токены (`session:jvmTest` — это A/B стенда
# на 200 прогонов). Включать по месту: `-PliveTests`, либо у себя в ~/.gradle/gradle.properties.
# Механизм и конвенция — LIVE_TESTS.md.